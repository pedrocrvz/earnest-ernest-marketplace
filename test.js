;(async () => {
    // load libraries
    require('dotenv').config()
    const awaitTransactionMined = require('await-transaction-mined')
    var Web3 = require('web3')
    this.encodeCall = require('../../test/helpers/encodeCall')
  
    var fs = re
})